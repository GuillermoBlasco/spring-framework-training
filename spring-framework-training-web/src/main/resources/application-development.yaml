spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/training
    username: training
    password: 1234
    driverClassName: org.postgresql.Driver

com:
  guillermoblasco:
    randomize:
      defaultSeed: 1000